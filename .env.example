# Local development placeholders only.
# Do not store real Cloudflare credentials or secrets in git.

# Production session secret:
#   wrangler secret put SESSION_SECRET

# Rate limiting requires a real Cloudflare KV namespace bound as RATE_LIMIT.
# Example:
#   wrangler kv namespace create RATE_LIMIT
#   wrangler kv namespace create RATE_LIMIT --preview
